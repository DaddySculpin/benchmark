import { themes } from '@coreym/benchmark';

export const Default = themes.default;
export const Dark = themes.dark;

// disabled other themes until they're ready
// export const Beige = themes.beige;
